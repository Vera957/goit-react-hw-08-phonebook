import { Login } from "components/Login";
import {Box} from '@chakra-ui/react'

export const LoginPage = () => {
    return (
        <Box h='100vh'>
            <title>Login</title>
            <Login />
        </Box>
    );
}
